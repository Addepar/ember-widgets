{{#if view.content.isGroupOption}}
  {{view.content.name}}
{{else}}
  {{#if view.titleOnOptions}}
    <span {{bind-attr title=view.label}}>
      {{view.label}}
    </span>
  {{else}}
    {{view.label}}
  {{/if}}
{{/if}}

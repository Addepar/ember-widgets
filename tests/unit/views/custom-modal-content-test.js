import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:custom-modal-content', 'CustomModalContentView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});

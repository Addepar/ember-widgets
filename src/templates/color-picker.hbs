{{partial colorPickerButtonPartial}}
{{#if shouldOpenPicker}}
  {{color-picker-dropdown
    dropdownClass=dropdownClass
    userSelected="userSelected"
    setSelectedColor="setSelectedColor"
    customColor=customColor
    selectedColor=selectedColor
  }}
{{/if}}
